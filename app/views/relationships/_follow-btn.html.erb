<% if current_user.following?(user) %>
  <%= link_to 'フォロー解除', user_relationships_path(user.id), class: "btn btn-danger",  remote: true, method: :delete %>
<% else %>
  <%= link_to 'フォローする', user_relationships_path(user.id), class: "btn btn-success", remote: true, method: :post %>
<% end %>
